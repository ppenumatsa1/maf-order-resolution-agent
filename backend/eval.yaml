name: order-resolution-parity
dataset:
  local_uri: .foundry/datasets/order-resolution-hosted-cases.jsonl
agent:
  name: order-resolution-hosted
evaluators:
  - relevance
  - task_adherence
options:
  pass_threshold: 0.8
